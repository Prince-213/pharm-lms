# Amplify Hosting + pnpm: prefer a flat node_modules tree so the build
# container and Next.js tracing resolve packages without deep store symlinks.
# See: https://docs.aws.amazon.com/amplify/latest/userguide/monorepo-configuration.html
node-linker=hoisted
