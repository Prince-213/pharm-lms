# Netlify + Auth.js: trust Host / X-Forwarded-* headers so the app URL matches
# the browser (production, branch deploys, deploy previews) without requiring
# a separate AUTH_URL per hostname when unset.
[build.environment]
  AUTH_TRUST_HOST = "true"
