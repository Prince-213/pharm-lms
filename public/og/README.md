# Open Graph images

Social sharing previews (target **1200×630**). Wired in `lib/site-metadata.ts`.

| File | Used for |
|------|----------|
| `default.jpg` | Site-wide Open Graph / Twitter card fallback |
| `home.jpg` | Homepage (`app/page.tsx`) — listed first in metadata |

Replace these JPEGs anytime; keep the same filenames or update `ogImagePaths` in `lib/site-metadata.ts`.

Brand: navy `#1e40af`, purple accent `#7a4dfc`, current PharmLMS book + caduceus logo.
